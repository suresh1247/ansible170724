---
- name: Reboot Windows Servers
  hosts: windows
  gather_facts: no

  tasks:

  - name: Reboot the Windows server
    win_reboot:
      reboot_timeout: 600
      pre_reboot_delay: 5
      post_reboot_delay: 30
      test_command: whoami
